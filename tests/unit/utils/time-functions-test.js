import timeFunctions from 'tabata/utils/time-functions';
import { module, test } from 'qunit';

module('Unit | Utility | time-functions', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = timeFunctions();
    assert.ok(result);
  });
});
